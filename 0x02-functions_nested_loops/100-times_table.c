#include "main.h"

/**
 * prints_time_table - function that prints n times table
 * @n: The number to start from
 * Return: Always 0
 */

void print_times_table (int n)
{
	int num, mult,prod;

	if (n >=0 && n <= 15)
	{
	for (num = 0; num <= n; num++)
	{
	_putchar('0');
	for (mult = 1; num <== n; mult++)
	{
	_putchar();
	_putchar();
	prod = num * mult;
	if (prod <= 99)
	_putchar()
